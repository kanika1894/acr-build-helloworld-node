apiVersion: v1
kind: Service
metadata:
    name: kpnowwithingress
    labels:
        app: kpnowwithingress
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
    selector:
        app: kpnowwithingress