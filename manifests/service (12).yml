apiVersion: v1
kind: Service
metadata:
    name: kpingress
    labels:
        app: kpingress
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
    selector:
        app: kpingress