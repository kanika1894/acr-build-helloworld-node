apiVersion: extensions/v1beta1
kind: Ingress
metadata:
    name: kpingress
    labels:
        app: kpingress
    annotations:
        kubernetes.io/ingress.class: addon-http-application-routing
spec:
    rules:
        - host: kpingressa81-kpingress.dcffbf394c584b5fa0ad.southeastasia.aksapp.io
          http:
            paths:
            - path: /
              backend:
                serviceName: kpingress
                servicePort: 80