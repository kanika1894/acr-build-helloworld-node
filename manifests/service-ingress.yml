apiVersion: v1
kind: Service
metadata:
    name: kpnowwithingress
    labels:
        app: kpnowwithingress
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: kpnowwithingress
